name: backend_core
description: Shared backend models, Firebase services, auth logic, and repositories for Lingua Neural apps.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.12
  firebase_auth: ^5.3.0
  firebase_core: ^3.15.2
  flutter_tts: ^4.2.5
  just_audio: ^0.10.0
  speech_to_text: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
